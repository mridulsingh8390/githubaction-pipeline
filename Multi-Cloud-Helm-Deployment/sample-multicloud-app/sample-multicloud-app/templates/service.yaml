apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-multicloud-app.fullname" . }}
  labels:
    {{- include "sample-multicloud-app.labels" . | nindent 4 }}
  {{- with .Values.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-multicloud-app.selectorLabels" . | nindent 4 }}
