name: Azure Terraform Import

on:
  workflow_dispatch:
    inputs:
      resource_group:
        description: "Resource Group to import"
        required: true
        type: string
<<<<<<< HEAD
      environment:
        description: "Environment label"
        required: true
        type: choice
        options:
          - dev
          - qa
          - staging
          - prod
=======
>>>>>>> b04bb3bd00ca3209480349840420032105625663
      action:
        description: "Action"
        required: true
        type: choice
        options:
          - discover
          - generate
          - discover-generate-validate
      tag_filter:
<<<<<<< HEAD
        description: "Filter by tag key=value (optional, leave empty for all)"
=======
        description: "Tag filter key=value (optional)"
>>>>>>> b04bb3bd00ca3209480349840420032105625663
        required: false
        type: string
        default: ""
      branch:
        description: "Branch"
        required: true
        type: string
        default: "main"

jobs:
<<<<<<< HEAD

=======
>>>>>>> b04bb3bd00ca3209480349840420032105625663
  discover:
    name: "Discover Resources"
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      resource_count: ${{ steps.discover.outputs.resource_count }}
<<<<<<< HEAD
      resource_types: ${{ steps.discover.outputs.resource_types }}
=======
>>>>>>> b04bb3bd00ca3209480349840420032105625663
    env:
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

<<<<<<< HEAD
      - name: List all resources in resource group
        run: |
          az resource list \
            --resource-group "${{ github.event.inputs.resource_group }}" \
            --output json > /tmp/all-resources-raw.json
          echo "Raw resource count:" $(python3 -c "import json; print(len(json.load(open('/tmp/all-resources-raw.json'))))")

      - name: Filter and generate discovery report
        id: discover
        env:
          TAG_FILTER: ${{ github.event.inputs.tag_filter }}
          RESOURCE_GROUP: ${{ github.event.inputs.resource_group }}
        run: |
          python3 scripts/discover.py
          echo "resource_count=$(cat /tmp/resource_count.txt)" >> "$GITHUB_OUTPUT"
          echo "resource_types=$(cat /tmp/resource_types.txt)" >> "$GITHUB_OUTPUT"

      - name: Upload discovery report
        uses: actions/upload-artifact@v4
        with:
          name: discovery-report-${{ github.run_id }}
          path: /tmp/discovery-report.json
=======
      - name: Discover
        id: discover
        run: |
          RG="${{ github.event.inputs.resource_group }}"
          TAG_FILTER="${{ github.event.inputs.tag_filter }}"

          az resource list --resource-group "$RG" --output json > /tmp/raw.json

          if [ -n "$TAG_FILTER" ]; then
            TAG_KEY=$(echo "$TAG_FILTER" | cut -d'=' -f1)
            TAG_VAL=$(echo "$TAG_FILTER" | cut -d'=' -f2)
            python3 -c "
import json
with open('/tmp/raw.json') as f: r=json.load(f)
out=[x for x in r if x.get('tags',{}).get('$TAG_KEY')=='$TAG_VAL']
print(f'Matched {len(out)} of {len(r)}')
json.dump(out, open('/tmp/resources.json','w'), indent=2)
"
          else
            cp /tmp/raw.json /tmp/resources.json
          fi

          python3 -c "
import json
from collections import defaultdict
r=json.load(open('/tmp/resources.json'))
by_type=defaultdict(list)
for x in r: by_type[x['type']].append(x['name'])
print(f'Total: {len(r)}')
for t,names in sorted(by_type.items()):
    print(f'  {t}')
    for n in names: print(f'    - {n}')
json.dump({'total':len(r),'by_type':dict(by_type)}, open('/tmp/report.json','w'), indent=2)
"
          COUNT=$(python3 -c "import json; print(json.load(open('/tmp/resources.json')).__len__())")
          echo "resource_count=$COUNT" >> "$GITHUB_OUTPUT"

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: discovery-report-${{ github.run_id }}
          path: /tmp/report.json
>>>>>>> b04bb3bd00ca3209480349840420032105625663
          retention-days: 30

  generate:
    name: "Generate Terraform Code"
    runs-on: ubuntu-latest
    needs: discover
    if: |
      github.event.inputs.action == 'generate' ||
      github.event.inputs.action == 'discover-generate-validate'
    permissions:
      id-token: write
      contents: read
    env:
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install aztfexport
        run: |
          curl -sL "https://github.com/Azure/aztfexport/releases/download/v0.15.0/aztfexport_linux_amd64.zip" -o /tmp/aztfexport.zip
          unzip -q /tmp/aztfexport.zip -d /tmp/aztfexport
          sudo mv /tmp/aztfexport/aztfexport /usr/local/bin/
          aztfexport --version

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.9"

<<<<<<< HEAD
      - name: Generate Terraform code with aztfexport
=======
      - name: Generate
>>>>>>> b04bb3bd00ca3209480349840420032105625663
        run: |
          mkdir -p /tmp/generated-tf
          aztfexport resource-group \
            --non-interactive \
            --output-dir /tmp/generated-tf \
            --overwrite \
            "${{ github.event.inputs.resource_group }}"
<<<<<<< HEAD
          echo "Generated files:"
          ls -la /tmp/generated-tf

      - name: Add S3 backend config
        run: bash scripts/add_backend.sh

      - name: Upload generated Terraform code
=======
          ls -la /tmp/generated-tf

      - name: Upload generated code
>>>>>>> b04bb3bd00ca3209480349840420032105625663
        uses: actions/upload-artifact@v4
        with:
          name: generated-tf-${{ github.run_id }}
          path: /tmp/generated-tf/
          retention-days: 30

  validate:
<<<<<<< HEAD
    name: "Validate with Terraform Plan"
=======
    name: "Validate"
>>>>>>> b04bb3bd00ca3209480349840420032105625663
    runs-on: ubuntu-latest
    needs: [discover, generate]
    if: github.event.inputs.action == 'discover-generate-validate'
    permissions:
      id-token: write
      contents: read
    env:
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: "true"
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.9"

      - name: Download generated code
        uses: actions/download-artifact@v4
        with:
          name: generated-tf-${{ github.run_id }}
          path: /tmp/generated-tf

      - name: Terraform Init
        working-directory: /tmp/generated-tf
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=azure/imports/${{ github.event.inputs.resource_group }}/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_STATE_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Terraform Plan
        working-directory: /tmp/generated-tf
        run: |
          terraform plan \
            -var="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
<<<<<<< HEAD
            -out=import-plan 2>&1 | tee /tmp/plan-output.txt
          grep -E "Plan:|No changes|to import|to add|to change|to destroy" /tmp/plan-output.txt || true

      - name: Upload plan output
        uses: actions/upload-artifact@v4
        with:
          name: plan-output-${{ github.run_id }}
          path: /tmp/plan-output.txt
=======
            2>&1 | tee /tmp/plan.txt
          grep -E "Plan:|No changes|to import" /tmp/plan.txt || true

      - name: Upload plan
        uses: actions/upload-artifact@v4
        with:
          name: plan-output-${{ github.run_id }}
          path: /tmp/plan.txt
>>>>>>> b04bb3bd00ca3209480349840420032105625663
          retention-days: 30
